
获取到的表中的数据为 QString 类型,这个数据保存了表中的内容和某些格式:

整个数据用行分隔符拆分，拆分为一个数组 :
1.第1个元素保存了整个表的行数和列数
2.之后的每个数组元素是表的一行数据，而每一行数据又可用列分隔符拆分为一个数组:
    I.每个元素对应此行一列的数据，此数据又用格式分隔符分成数组：
        a.第一个元素保存了这个此行此列的单元格的信息，中间用“，”分隔开： 行号，列号，占行数+占列数，单元格宽度*单元格高度
        b.第二个元素保存了此单元格的内容
